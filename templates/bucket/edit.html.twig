{% extends 'base.html.twig' %}

{% block title %}Edit Bucket{% endblock %}

{% block main %}
    <h1>Edit Bucket</h1>

    <a href="{{ path('app_bucket_test_add', {'id': bucket.id}) }}" class="btn btn-primary">add bucket</a>

    {{ include('bucket/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_bucket_index') }}">back to list</a>

    {{ include('bucket/_delete_form.html.twig') }}
{% endblock %}
